package calculator;

class StringCalculator {

    public int add(String input) {
    	if(input.isEmpty())
    	{
    		return 0;
    	}
        return SingleInt(input);
    }
    
    private int SingleInt(String input)
    {
    	return Integer.parseInt(input);
    }

}
